import { getCurrentUser } from "@/lib/auth"
export default async function AccountPage() { const user = await getCurrentUser(); return <><p className="text-sm font-black tracking-[.16em] text-primary uppercase">CMS</p><h1 className="mt-2 text-3xl font-black">Account</h1><div className="mt-8 max-w-lg rounded-xl border bg-card p-5"><p className="font-bold">{user?.name}</p><p className="mt-1 text-sm text-muted-foreground">{user?.email}</p><p className="mt-4 text-xs font-bold tracking-wide text-primary">ROLE: {String((user as { role?: string } | null)?.role ?? "ADMIN")}</p></div></> }
