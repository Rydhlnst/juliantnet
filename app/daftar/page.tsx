import type { Metadata } from "next"
import { RegistrationForm } from "@/components/registration-form"
import { SiteFooter } from "@/components/site-footer"
import { SiteHeader } from "@/components/site-header"
export const metadata: Metadata = { title: "Daftar Internet Cepat", description: "Mulai permintaan pemasangan Internet Cepat setelah coverage dan paket sesuai kebutuhanmu.", alternates: { canonical: "/daftar" } }
export default function RegistrationPage() { const steps = ["Cek Coverage", "Pilih Paket", "Data Pelanggan", "Alamat Instalasi", "Konfirmasi"]; return <><SiteHeader /><main className="mx-auto max-w-3xl px-5 py-16"><p className="text-sm font-black tracking-[.16em] text-primary uppercase">Pendaftaran</p><h1 className="mt-3 text-4xl font-black tracking-tight">Mulai pemasangan dengan langkah yang jelas.</h1><ol className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-5 sm:gap-2">{steps.map((step, index) => <li key={step} className={index === 2 ? "border-t-2 border-primary/70 pt-2 text-xs font-bold text-primary" : "border-t border-border pt-2 text-xs text-muted-foreground"}>0{index + 1}<span className="mt-1 block">{step}</span></li>)}</ol><div className="mt-10 rounded-2xl border border-border bg-card p-6 sm:p-8"><h2 className="text-xl font-black">Data pelanggan</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Isi data awal agar tim Internet Cepat dapat membantu menyiapkan paket dan jadwal pemasangan yang sesuai.</p><div className="mt-7"><RegistrationForm /></div></div></main><SiteFooter /></> }